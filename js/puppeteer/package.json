{
  "name": "watcher-automation-puppeteer-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "autoAnalyzeMode": "mocha --reporter allure-mocha tests/autoAnalyzeMode.js --timeout 180s",
    "manualMode": "mocha --reporter allure-mocha tests/manualMode.js --timeout 180s",
    "axeConfigurations": "mocha --reporter allure-mocha tests/axeConfigurations.js --timeout 180s",
    "excludeURLs": "mocha --reporter allure-mocha tests/excludeURLs.js --timeout 180s",
    "configureOverrides": "mocha --reporter allure-mocha tests/configureOverrides.js --timeout 180s",
    "wrapMethods": "mocha --reporter allure-mocha tests/wrapMethods.js --timeout 180s",
    "edgeCases": "mocha --reporter allure-mocha tests/edgeCases.js --timeout 180s",
    "test-all": "(npm run autoAnalyzeMode || true) && (npm run manualMode || true) && (npm run axeConfigurations || true) && (npm run excludeURLs || true) && (npm run configureOverrides || true) && (npm run wrapMethods || true) && (npm run edgeCases || true)"
  },
  "devDependencies": {
    "@axe-core/watcher": "4.0.0-rc.0f7eb327",
    "@types/mocha": "^10.0.10",
    "allure-commandline": "^2.34.1",
    "allure-mocha": "^3.3.2",
    "chai": "^5.3.1",
    "mocha": "^11.7.1",
    "mocha-allure-reporter": "^1.4.0",
    "mochawesome": "^7.1.3",
    "puppeteer": "^24.16.2"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "dotenv": "^17.2.1",
    "winston": "^3.18.3"
  }
}
